<h1>Sessions#login</h1>
<%= render 'layouts/error', { obj: @user } if @user %>

<%= form_tag(login_path) do %>
  Email: <%= text_field_tag 'session[email]'%>
  Password: <%= password_field_tag 'session[password]'%>
  <%= submit_tag('Login') %>
<% end %>